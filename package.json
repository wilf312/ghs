{
  "name": "ghs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "deno test",
    "run": "deno run --allow-run --allow-read ghs.ts",
    "addCli": "npm run compile && npm run addCommand",
    "compile": "deno compile --allow-run --allow-read --output ghs ghs.ts",
    "addCommand": "sudo mv ghs /usr/local/bin/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@inquirer/prompts": "^7.2.1",
    "inquirer-autocomplete-prompt": "^3.0.1"
  }
}
